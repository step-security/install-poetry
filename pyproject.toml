[tool.poetry]
name = "install-poetry"
version = "1"
description = ""
authors = ["step-security"]
maintainers = ["step-security"]

[tool.poetry.dependencies]
python = "^3.10"
pytest = "^9.0.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
